import { Meta, Story, Canvas, ArgsTable, Typeset } from "@storybook/addon-docs";
import tokens from "../dist/index.json";
import config from "../src/config.json";

<Meta title={`${config.name}/Typography`} />

# Typography

## Fonts

### RijksoverheidSans

<Typeset
  fontFamily={tokens["duo"]["typography"]["sans-serif"]["font-family"].value}
  fontSizes={["24px"]}
  sampleText="The Quick Brown Fox Jumps Over The Lazy Dog"
/>

## Font size scale

<Typeset
  fontFamily={tokens["duo"]["typography"]["sans-serif"]["font-family"].value}
  fontSizes={Object.values(tokens["duo"]["typography"]["scale"])
    .map((sizeTokens) => sizeTokens["font-size"])
    .map(({ value }) => value)}
  sampleText="The Quick Brown Fox Jumps Over The Lazy Dog"
/>
