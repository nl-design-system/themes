import { Meta } from "@storybook/addon-docs/blocks";
import tokens from "../dist/tokens.json";
import { ColorSearch } from "@nl-design-system-unstable/theme-toolkit/src/ColorSearch";
import { ColorTable } from "@nl-design-system-unstable/theme-toolkit/src/ColorTable";
import config from "../src/config.json";

<Meta title="Zaanstad/Color" />

# Kleuren

## Vind een kleur

<ColorSearch tokens={{ ...tokens["basis"]["color"], ...(tokens[config.prefix] ? ["color"] : {}) }} />

## Huisstijl kleurenpalet

Deze kleuren vormen de brand-laag van de design tokens en worden gebruikt door de basis tokens in de common-laag.

**Tip** Door de componenten niet direct aan deze tokens de koppelen blijft het thema makkelijker te onderhouden.

<ColorTable tokens={tokens[config.prefix] ? ["color"] : {}} />
