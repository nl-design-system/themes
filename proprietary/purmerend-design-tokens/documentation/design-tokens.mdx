import { Meta } from "@storybook/blocks";
import { DesignTokensTable } from "@nl-design-system-unstable/theme-toolkit/src/DesignTokensTable";
import tokens from "../dist/list.json";
import config from "../src/config.json";

<Meta title="Voorbeeld Thema/Design Tokens" />

# Design Tokens

<DesignTokensTable tokens={tokens.filter((token) => token.path[0] !== config.prefix)} />

## Proprietary Design Tokens

<DesignTokensTable tokens={tokens.filter((token) => token.path[0] === config.prefix)} />
