import { Meta, Story, Canvas, ArgsTable, Typeset } from "@storybook/addon-docs";
import tokens from "../dist/index.json";
import config from "../src/config.json";

<Meta title={`${config.name}/Typography`} />

# Typography

## Asap

### Regular

<Typeset
  fontFamily={tokens["dso"]["typography"]["sans-serif"]["font-family"].value}
  fontSizes={["24px"]}
  sampleText="The Quick Brown Fox Jumps Over The Lazy Dog"
/>

### Semi-bold

<Typeset
  fontFamily={tokens["dso"]["typography"]["sans-serif"]["font-family"].value}
  fontWeight={600}
  fontSizes={["24px"]}
  sampleText="The Quick Brown Fox Jumps Over The Lazy Dog"
/>

### Bold

<Typeset
  fontFamily={tokens["dso"]["typography"]["sans-serif"]["font-family"].value}
  fontWeight={700}
  fontSizes={["24px"]}
  fontStyle="italic"
  sampleText="The Quick Brown Fox Jumps Over The Lazy Dog"
/>

### Italic

Italic is also available, but `fontStyle` is currently not supported by the `<Typeset>` component to demonstrate an example.
