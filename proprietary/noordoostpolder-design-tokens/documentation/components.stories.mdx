import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import {
  defaultArgs,
  argTypes,
  getComponentArgs,
  Template,
} from "@nl-design-system-unstable/theme-toolkit/src/components";
import config from "../src/config.json";

<Meta
  title={`${config.name}/Components`}
  argTypes={argTypes}
  decorators={[(story) => `<div class="utrecht-document ${config.prefix}-theme">${story()}</div>`]}
  parameters={{
    docs: {
      transformSource: (_src, { args }) => Template(args),
    },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Components

<Canvas>
  <Story
    name="Components"
    args={{
      ...defaultArgs,
      ...getComponentArgs(config),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Components" />
