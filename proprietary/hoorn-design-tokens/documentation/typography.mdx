import { Meta, Typeset } from "@storybook/addon-docs/blocks";
import tokens from "../dist/tokens.json";
import config from "../src/config.json";

<Meta title="Hoorn/Typography" />

# Typography

## Clear Sans

Clear Sans is a free font and is available in the [`@fontsource/clearsans` npm package](https://www.npmjs.com/package/@fontsource/clear-sans).

### Regular

<Typeset
  fontFamily={tokens["hoorn"]["typography"]["sans-serif"]["font-family"].value}
  fontWeight={400}
  fontSizes={["24px"]}
  sampleText="The Quick Brown Fox Jumps Over The Lazy Dog"
/>

### Semi-bold

<Typeset
  fontFamily={tokens["hoorn"]["typography"]["sans-serif"]["font-family"].value}
  fontWeight={500}
  fontSizes={["24px"]}
  fontStyle="italic"
  sampleText="The Quick Brown Fox Jumps Over The Lazy Dog"
/>

### Bold

<Typeset
  fontFamily={tokens["hoorn"]["typography"]["sans-serif"]["font-family"].value}
  fontWeight={700}
  fontSizes={["24px"]}
  fontStyle="italic"
  sampleText="The Quick Brown Fox Jumps Over The Lazy Dog"
/>

### Italic

Regular Italic is also available, but `fontStyle` is currently not supported by the `<Typeset>` component to demonstrate an example.
