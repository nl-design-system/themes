import { Meta, Story, Canvas, ArgsTable, Description, Source } from "@storybook/addon-docs";

<!-- Import component and component styles -->

import "./bem.scss";

<!-- Import Docs -->

import README from "./README.md";

export const Template = ({ href, content }) =>
  `<button class="example-button" type="button"><span class="example-button__text">${content}</span></button>`;

<Meta
  title="CSS Component/Button"
  argTypes={{
    content: {
      description: "Set the text content",
      control: "text",
    },
  }}
  parameters={{
    docs: {
      transformSource: (_src, { args }) => Template(args),
    },
    status: "IN DEVELOPMENT",
    notes: { UX: README },
  }}
/>

# Example Button

<Canvas>
  <Story name="Button" args={{ content: "Press me!" }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Button" />
